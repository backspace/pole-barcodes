{{outlet}}

{{#if this.barcode}}
  Found a barcode??? {{this.barcode}}
{{/if}}

{{#if this.error}}
  An error occurred: {{this.error}}
{{/if}}

<BarcodeReader @onBarcodeRead={{action (mut barcode)}} @onError={{action (mut error)}} />
